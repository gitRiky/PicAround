<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.project.pervsys.picaround.activity.InfoActivity$PlaceholderFragment">

    <TextView
        android:id="@+id/info_app_name"
        android:layout_width="200dp"
        android:layout_height="35dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="37dp"
        android:textAlignment="center"
        android:textSize="30sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@mipmap/icon"
        android:layout_below="@+id/info_app_version"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp" />

    <TextView
        android:id="@+id/info_app_version"
        android:layout_width="200dp"
        android:layout_height="20dp"
        android:textAlignment="center"
        android:textStyle="italic"
        android:layout_below="@+id/info_app_name"
        android:layout_alignStart="@+id/info_app_name" />

    <TextView
        android:id="@+id/info_app_details"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/info_app_version"
        android:layout_below="@+id/imageView"
        android:layout_marginTop="28dp"
        android:textAlignment="center" />

</RelativeLayout>
